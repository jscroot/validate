# validate
Validate Input

## Release

Tag version

```sh
git tag                                  #check current version
git tag v0.0.1                           #set tag version
git push origin --tags  
```